Домашняя работа

Создать следующие функции

1. Функция сложения: принимает на вход 2 числа, Возвращает результат, Выводит на экран результат их сложения
2. Функция вычитания: принимает на вход 2 числа, Возвращает результат, Выводит на экран со след строки результат умножения
3. Функция умножения: принимает на вход 2 числа, Возвращает результат, Выводит на экран со след строки результат умножения
4. Функция деления: принимает на вход 2 числа, Возвращает результат, Выводит на экран со след строки результат умножения (!! не делить на 0)
5. Фунцкия вывода строки: На вход принимает 3 параметра(имя фамилия отчество) , Возвращает результат, Вывести с новой строки результат вида (Иванов Иван Иванович)

Дополнительно
 1.   Посмотреть как работает в калькуляторе функция "%" и попробовать сделать такую же функцию
    На вход одно число, Вывод результата с новой строки


1. Создать массив от 0 до 9 и посчитать их сумму
2.
